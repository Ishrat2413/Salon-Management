"use client";

import { DashboardPage } from "@/components/layout/dashboard-page";

export default function AddEntryPage() {
  return (
    <DashboardPage
      title='Add Entry'
      description='Create a new service entry for the currently logged-in role.'
    />
  );
}
